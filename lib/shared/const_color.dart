import 'package:flutter/material.dart';

// We'll use this through out our project in other not to
// use colors.() in our prject
// Make sure to assign all colors here for refrerence

class ThemeColors {
  static const background = Color(0xFF80CBC4);
  static const white = Colors.white;
  static const backGradient = Color(0x00CDAC00);
  static const black = Color(0xFFFFFFFF);
}
